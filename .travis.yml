language: python

python:
    - 3.5
    - 3.6
    - nightly

env:
    - DJANGO_VERSION=1.10
    - DJANGO_VERSION=1.11

install:
    - pip install Django==$DJANGO_VERSION
    - pip install -r requirements.txt

script:
    - flake8
    - pytest


